# PP 在线聊天系统 — 完整实验报告

> **课程名称**：Web程序设计（Java EE）  
> **项目名称**：PP 在线聊天系统  
> **选题**：题目3 - 在线聊天系统（85分）  
> **完成日期**：2026年5月  

---

## 目录

1. [项目概述](#一项目概述)
2. [需求分析](#二需求分析)
3. [系统设计](#三系统设计)
4. [数据库设计](#四数据库设计)
5. [功能实现](#五功能实现)
6. [关键技术](#六关键技术)
7. [测试与验证](#七测试与验证)
8. [部署说明](#八部署说明)
9. [问题与解决](#九问题与解决)
10. [总结与展望](#十总结与展望)

---

## 一、项目概述

### 1.1 项目背景

PP 在线聊天系统是一个基于 Web 的即时通讯应用，采用 Spring Boot 4.0.6 + Java 21 技术栈开发。系统支持用户注册登录、好友管理（分组）、一对一私聊、多人群聊、语音消息、聊天记录查询与导出等核心功能。

### 1.2 评分目标对照

| 课程要求 | 对应功能模块 | 实现状态 |
|----------|-------------|---------|
| 登录与注册 | 用户认证模块 | ✅ 已实现 |
| 聊天管理（私聊、群聊） | 私聊模块 + 群聊模块 | ✅ 已实现 |
| 查询聊天记录，下载到本地 | 聊天记录搜索 + TXT 导出 | ✅ 已实现 |
| 语音聊天（+5分） | 语音消息模块 | ✅ 已实现 |
| 好友管理（分组、移动、删除、重新发送验证信息） | 好友管理模块 + 通知中心 | ✅ 已实现 |
| （附加）后台管理系统 | Admin 管理后台 | ✅ 已实现 |

### 1.3 技术栈

| 层次 | 技术选型 | 版本 |
|------|---------|------|
| Web框架 | Spring Boot | 4.0.6 |
| 编程语言 | Java | 21 |
| 持久层 | Spring Data JPA + Hibernate | - |
| 数据库 | MySQL | 8.0+ |
| 模板引擎 | Thymeleaf | - |
| 实时通信 | WebSocket STOMP | - |
| 前端客户端 | SockJS + StompJS | - |
| 密码加密 | BCrypt (Spring Security) | - |
| 代码简化 | Lombok | - |

---

## 二、需求分析

### 2.1 功能性需求

#### 2.1.1 用户认证
- 用户注册（用户名 + 密码 + 昵称）
- 用户登录（Session 鉴权，密码 BCrypt 加密）
- 用户登出（销毁 Session）
- 忘记密码（通过用户名重置密码）
- 记录登录次数和最近登录时间

#### 2.1.2 好友管理
- 搜索用户（按用户名/昵称模糊搜索）
- 发送好友申请（附验证消息）
- 同意/拒绝好友申请
- 好友分组管理（创建/重命名/删除）
- 移动好友到指定分组
- 设置好友备注名
- 删除好友（双向删除）

#### 2.1.3 私聊
- 从好友列表发起私聊
- 实时文本消息收发（WebSocket STOMP）
- 语音消息录制与发送（MediaRecorder API）
- 消息状态追踪（已发送 → 已送达 → 已读）
- 未读消息计数与红点提示
- 聊天记录关键词搜索
- 聊天记录导出为 TXT 文件

#### 2.1.4 群聊
- 创建群聊（选好友入群）
- 群消息实时收发（WebSocket 广播）
- 群成员管理（邀请/踢出/退出）
- 群公告编辑（仅群主）
- 解散群聊（仅群主）
- 群聊邀请（发送/接受/拒绝）
- 群聊天记录导出

#### 2.1.5 个人中心
- 查看个人资料（头像、昵称、登录统计）
- 修改昵称
- 修改密码（需验证旧密码）
- 主题切换（亮色/暗色/跟随系统）

### 2.2 非功能性需求

- **性能**：页面加载 < 2秒，WebSocket 延迟 < 500ms
- **安全**：BCrypt 密码加密，Session 认证，防 XSS/SQL 注入
- **可用性**：微信/QQ 风格界面，响应式设计，操作反馈及时
- **兼容性**：支持 Chrome/Firefox/Edge 等现代浏览器

---

## 三、系统设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      浏览器 (Browser)                        │
│  ┌──────────────────┐     ┌─────────────────────────────┐   │
│  │  Thymeleaf 页面   │     │   聊天 SPA (原生 JS)         │   │
│  │  登录/注册/管理   │     │   WebSocket 实时通信          │   │
│  └────────┬─────────┘     └──────────┬──────────────────┘   │
│           │ HTTP                     │ HTTP + WebSocket      │
└───────────┼──────────────────────────┼───────────────────────┘
            │                          │
┌───────────┼──────────────────────────┼───────────────────────┐
│           ▼      Spring Boot         ▼                       │
│  ┌─────────────────┐    ┌──────────────────────────┐        │
│  │ PageController  │    │ REST Controller          │        │
│  │ (Thymeleaf渲染) │    │ (JSON API)               │        │
│  └────────┬────────┘    └──────────┬───────────────┘        │
│           │                        │                        │
│  ┌────────┴────────────────────────┴──────────────┐         │
│  │              Service 层                         │         │
│  │  UserService / FriendService / ChatService /    │         │
│  │  GroupService / FileService                     │         │
│  └──────────────────┬──────────────────────────────┘         │
│                     │                                        │
│  ┌──────────────────┴──────────────────────────────┐         │
│  │            Repository 层 (JPA)                   │         │
│  └──────────────────┬──────────────────────────────┘         │
│                     │                                        │
│  ┌──────────────────┴──────────────────────────────┐         │
│  │          WebSocket (STOMP)                       │         │
│  │  ChatController — 消息转发                        │         │
│  └─────────────────────────────────────────────────┘         │
└─────────────────────┬────────────────────────────────────────┘
                      │
          ┌───────────┴───────────┐
          │    MySQL 数据库        │
          │   10张核心表           │
          └───────────────────────┘
```

### 3.2 包结构

```
com.ncu.pp/
├── PpApplication.java
├── config/                    # 配置类
│   ├── WebConfig.java         # Web配置（LoginInterceptor + AdminInterceptor）
│   └── WebSocketConfig.java   # WebSocket配置
├── entity/                    # 实体类（10个）
│   ├── User.java
│   ├── Admin.java
│   ├── FriendGroup.java
│   ├── Friend.java
│   ├── FriendRequest.java
│   ├── PrivateMessage.java
│   ├── GroupChat.java
│   ├── GroupMember.java
│   ├── GroupMessage.java
│   └── GroupInvitation.java   # 含 type 字段区分邀请/申请
├── repository/                # 数据访问层（10个）
├── service/                   # 业务逻辑层（6个）
│   ├── UserService.java       # 含级联删除
│   ├── AdminService.java
│   ├── FriendService.java
│   ├── ChatService.java
│   ├── GroupService.java      # 含入群申请
│   └── FileService.java
├── controller/                # 控制器层
│   ├── page/
│   │   ├── PageController.java
│   │   └── AdminController.java
│   ├── rest/
│   │   ├── FriendRestController.java
│   │   ├── ChatRestController.java
│   │   ├── GroupRestController.java   # 含入群申请API
│   │   ├── ProfileRestController.java
│   │   └── AdminRestController.java   # 后台管理API
│   └── websocket/
│       └── ChatController.java
├── interceptor/
│   ├── LoginInterceptor.java
│   └── AdminInterceptor.java
└── dto/
    ├── ApiResponse.java
    └── ChatMessage.java
```

### 3.3 UI 设计

**聊天主界面布局（微信/QQ 风格三栏）：**

```
┌──────┬────────────────┬──────────────────────────────────────┐
│ 💬   │ 🔍 搜索        │ 张三                          ⋯    │
│ 👥   │                │──────────────────────────────────────│
│ 🏠   │ 张三   14:32   │ 14:20                              │
│ ✉️   │ 好的，明天见！  │ ┌─────────────────┐                 │
│      │                │ │ 明天下午有空吗？ │ (对方·白色)     │
│  👤  │ 李四   13:15   │ └─────────────────┘                 │
│      │ [3] 你看到群   │         ┌──────────────┐            │
│      │ 计科2班 12:00  │         │ 有空，什么事？│ (我·绿色)  │
│      │                │         └──────────────┘            │
│      │                │ ┌─────────────────┐                 │
│      │                │ │ 好的，明天见！   │ (对方·白色)     │
│      │                │ └─────────────────┘                 │
│      │                │ ┌🔊 ─────── 0:05┐ (语音消息)        │
│      │                │ └───────────────┘                   │
│      │                │──────────────────────────────────────│
│      │                │ 😊 📎 🎤                            │
│      │                │ ┌──────────────────────┐ [发送]     │
│      │                │ │ 输入消息...           │            │
│      │                │ └──────────────────────┘            │
└──────┴────────────────┴──────────────────────────────────────┘
 左侧    中间会话列表       右侧聊天区域
 导航
```

**左侧导航栏：**
- 消息列表
- 好友列表（含添加好友）
- 群聊列表（含创建群、加入群）
- 通知中心（好友申请 + 群聊邀请 + 入群申请）
- 个人中心（底部用户头像）

**主题系统：** 支持亮色、暗色、跟随系统三种模式

---

## 四、数据库设计

### 4.1 核心表清单

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| pp_user | 用户表 | id, username, password, nickname, avatar |
| pp_friend_group | 好友分组表 | id, user_id, name |
| pp_friend | 好友关系表 | id, user_id, friend_id, group_id, remark |
| pp_friend_request | 好友申请表 | id, from_user_id, to_user_id, message, status |
| pp_private_message | 私聊消息表 | id, sender_id, receiver_id, content, msg_type, audio_data, status |
| pp_group_chat | 群聊表 | id, name, owner_id, notice |
| pp_group_member | 群成员表 | id, group_id, user_id, role |
| pp_group_message | 群聊消息表 | id, group_id, sender_id, content, msg_type, audio_data |
| pp_group_invitation | 群聊邀请/入群申请表 | id, group_id, from_user_id, to_user_id, type, status |
| pp_admin | 管理员表 | id, username, password, nickname |

### 4.2 ER 关系图

```
pp_user (1) ──────< pp_friend_group (N)
pp_user (1) ──────< pp_friend (N)
pp_user (1) ──────< pp_friend_request (N)
pp_user (1) ──────< pp_private_message (N)
pp_user (1) ──────< pp_group_member (N)
pp_user (1) ──────< pp_group_invitation (N)
pp_group_chat (1) ─< pp_group_member (N)
pp_group_chat (1) ─< pp_group_message (N)
pp_group_chat (1) ─< pp_group_invitation (N)
pp_admin (独立)    — 管理员认证独立于用户体系
```

### 4.3 关键索引

- `pp_user.username` — 唯一索引
- `pp_friend.(user_id, friend_id)` — 复合唯一索引
- `pp_group_member.(group_id, user_id)` — 复合唯一索引
- `pp_private_message.(sender_id, receiver_id)` — 复合索引
- `pp_group_message.group_id` — 普通索引

---

## 五、功能实现

### 5.1 模块1：账户系统（陈弦）

**核心代码位置：**
- `UserService.java` — 注册、登录、密码管理、级联删除
- `PageController.java` — 页面路由
- `ProfileRestController.java` — 个人资料 API
- `LoginInterceptor.java` — 登录拦截
- `chat-profile.js` — 个人中心前端

**关键实现：**
```java
// 密码加密
user.setPassword(passwordEncoder.encode(password));

// 登录验证
if (!passwordEncoder.matches(password, user.getPassword())) {
    return null;
}

// Session 存储
session.setAttribute("currentUser", user);

// 级联删除用户所有关联数据
@Transactional
public void deleteUserAndData(Long userId) {
    // 删除用户创建的群聊、群消息、群成员、群邀请
    // 删除私聊消息、好友关系、好友分组、好友请求
    userRepository.deleteById(userId);
}
```

### 5.2 模块2：好友管理（齐睿）

**核心代码位置：**
- `FriendService.java` — 好友业务逻辑
- `FriendRestController.java` — REST API
- `chat-friends.js` — 好友列表、通知中心前端

**关键实现：**
```java
// 双向添加好友
addFriend(fromId, toId, fromGroup.getId());
addFriend(toId, fromId, toGroup.getId());

// 删除分组前迁移好友
List<Friend> friendsInGroup = friendRepository.findByGroupId(groupId);
for (Friend friend : friendsInGroup) {
    friend.setGroupId(null);
    friendRepository.save(friend);
}
```

**通知中心：** 合并好友申请、群聊邀请、入群申请三种通知，统一展示在左栏。

### 5.3 模块3：群聊管理（沈越）

**核心代码位置：**
- `GroupService.java` — 群聊业务逻辑
- `GroupRestController.java` — REST API
- `chat-groups.js` — 群列表、创建群、成员管理前端

**关键实现：**
```java
// 入群申请（区分邀请与申请）
GroupInvitation inv = new GroupInvitation();
inv.setType(1); // 1=申请
inv.setFromUserId(userId);
inv.setToUserId(group.getOwnerId());

// 群主权限校验
if (!group.getOwnerId().equals(user.getId())) {
    throw new IllegalArgumentException("只有群主可以解散群");
}
```

### 5.4 模块4：消息系统（宋佳伟）

**核心代码位置：**
- `ChatService.java` — 私聊业务逻辑
- `ChatRestController.java` — REST API
- `ChatController.java` (websocket) — STOMP 消息处理
- `WebSocketConfig.java` — WebSocket 配置
- `chat-core.js` — WebSocket 连接/重连、消息发送
- `chat-view.js` — 聊天界面渲染

**WebSocket 通信流程：**
```
用户A → STOMP /app/chat/private → ChatController
  → 存入 private_message 表
  → 推送 /user/{receiverId}/queue/private → 用户B

用户A → STOMP /app/chat/group → ChatController
  → 存入 group_message 表
  → 广播 /topic/group/{groupId} → 所有在线成员
```

**消息状态机制：**
- 发送时：status = 0（已发送）
- 对方收到推送：status = 1（已送达）
- 对方打开会话：status = 2（已读）

### 5.5 模块5：语音消息（郭峰）

**核心代码位置：**
- `chat-view.js` — 前端录音逻辑
- `ChatController.java` — 后端音频处理
- `FileService.java` — 文件存储

**技术流程：**
```javascript
// 前端录音
const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
const recorder = new MediaRecorder(stream, { mimeType: 'audio/webm;codecs=opus' });
recorder.onstop = () => {
    const blob = new Blob(chunks, { type: 'audio/webm' });
    const reader = new FileReader();
    reader.onloadend = () => {
        // Base64 编码后通过 STOMP 发送
        stompClient.send('/app/chat/private', {}, JSON.stringify({
            msgType: 1,
            audioData: reader.result
        }));
    };
    reader.readAsDataURL(blob);
};
```

**STOMP 缓冲区调优：** 语音 Base64 数据 (~68KB) 超过默认 64KB 限制，通过配置 WebSocketTransport 1MB 缓冲区解决。

### 5.6 模块6：后台管理 + 前端UI（李子旸）

**后台管理系统：**
- `Admin.java` — 管理员实体
- `AdminService.java` — 管理员登录、默认管理员初始化
- `AdminRestController.java` — 9张表 CRUD + 仪表盘统计
- `AdminInterceptor.java` — 管理员权限校验
- `admin.html` — 后台管理 SPA

**前端UI系统：**
- `chat.html` — 三栏布局 HTML + CSS
- `style.css` — 主题系统（亮色/暗色/跟随系统）
- `theme.js` — 主题管理器
- `chat-ui.js` — 粒子效果、分割线、模态框、Toast
- `landing.html` — Landing 首页

---

## 六、关键技术

### 6.1 WebSocket STOMP 实时通信

**配置要点：**
```java
@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {
    @Override
    public void configureMessageBroker(MessageBrokerRegistry config) {
        config.enableSimpleBroker("/topic", "/queue");
        config.setApplicationDestinationPrefixes("/app");
    }

    @Override
    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint("/ws")
                .setAllowedOriginPatterns("*")
                .addInterceptors(new AuthHandshakeInterceptor())
                .withSockJS()
                .setSuppressCors(true);
    }
}
```

**认证握手：**
```java
static class AuthHandshakeInterceptor implements HandshakeInterceptor {
    @Override
    public boolean beforeHandshake(..., Map<String, Object> attributes) {
        HttpSession session = servletRequest.getServletRequest().getSession(false);
        if (session != null && session.getAttribute("currentUser") != null) {
            attributes.put("currentUser", session.getAttribute("currentUser"));
            return true;
        }
        return false;
    }
}
```

### 6.2 BCrypt 密码加密

```java
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
}

// 加密
String hashed = passwordEncoder.encode(rawPassword);

// 验证
boolean matches = passwordEncoder.matches(rawPassword, hashed);
```

### 6.3 JPA 批量查询优化

```java
// 避免 N+1 查询：批量获取未读消息数
@Query("SELECT m.senderId, COUNT(m) FROM PrivateMessage m " +
       "WHERE m.receiverId = :receiverId AND m.status = 0 GROUP BY m.senderId")
List<Object[]> countUnreadGroupedBySender(@Param("receiverId") Long receiverId);

// 批量加载用户信息
List<Long> senderIds = messages.stream()
    .map(PrivateMessage::getSenderId).distinct().toList();
Map<Long, User> userMap = userRepository.findAllById(senderIds).stream()
    .collect(Collectors.toMap(User::getId, u -> u));
```

### 6.4 异步视图加载竞态条件处理

```javascript
let viewGeneration = 0;

function switchView(view) {
    currentView = view;
    const gen = ++viewGeneration;  // 递增版本号
    switch (view) {
        case 'chat': loadChatView(gen); break;
        case 'friends': loadFriendsView(gen); break;
    }
}

async function loadChatView(gen) {
    const isStale = () => gen !== viewGeneration;
    const res = await fetch('/api/friends');
    if (isStale()) return;  // 检查是否过期
    // ... 继续处理
}
```

### 6.5 MediaRecorder 语音录制

```javascript
const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
const recorder = new MediaRecorder(stream, { mimeType: 'audio/webm;codecs=opus' });
recorder.start();
// ... 录音中 ...
recorder.stop();
recorder.onstop = () => {
    const blob = new Blob(chunks, { type: 'audio/webm' });
    // 转为 Base64 发送
};
```

---

## 七、测试与验证

### 7.1 单元测试

**测试框架：** JUnit 5 + Mockito

**测试结果：**
```
Tests run: 56, Failures: 0, Errors: 0, Skipped: 0

UserServiceTest:     11 tests passed
FriendServiceTest:   13 tests passed
ChatServiceTest:      7 tests passed
GroupServiceTest:    14 tests passed
Controller Tests:    11 tests passed
```

**测试覆盖率：** ~78%

### 7.2 集成测试

- ChatRestController 接口测试
- ProfileRestController 接口测试
- ApiExceptionHandler 异常处理测试

### 7.3 功能测试

| 功能模块 | 测试项 | 结果 |
|---------|--------|------|
| 用户认证 | 注册/登录/登出/忘记密码 | ✅ 通过 |
| 好友管理 | 搜索/申请/分组/移动/删除 | ✅ 通过 |
| 私聊 | 文本/语音/搜索/导出 | ✅ 通过 |
| 群聊 | 创建/消息/成员/邀请/解散 | ✅ 通过 |
| 个人中心 | 资料/昵称/密码/主题 | ✅ 通过 |

### 7.4 性能测试

- 并发用户数：50
- 平均响应时间：< 500ms
- WebSocket 消息延迟：< 200ms
- 错误率：< 1%

---

## 八、部署说明

### 8.1 环境要求

- JDK 21
- MySQL 8.0+
- Maven 3.8+

### 8.2 打包部署

```bash
# 打包
./mvnw clean package -DskipTests

# 运行
java -jar target/pp-0.0.1-SNAPSHOT.jar

# 生产环境（带配置文件）
java -jar pp-0.0.1-SNAPSHOT.jar --spring.config.location=./application.yml
```

### 8.3 线上配置

```yaml
server:
  port: 8443
  ssl:
    enabled: true
    key-store: file:/opt/pp-chat/keystore.p12
    key-store-password: your_password
    key-store-type: PKCS12

spring:
  datasource:
    url: jdbc:mysql://mysql6.sqlpub.com:3311/gove_sqlpub?...
    username: iamgove
    password: AtnWe0aw7DAwB0gX
  websocket:
    messaging:
      send-buffer-size-limit: 1048576
      message-size-limit: 1048576
  messaging:
    stomp:
      buffer-size-limit: 1048576

app:
  upload-dir: /var/lib/pp-chat/uploads/
```

---

## 九、问题与解决

### 9.1 STOMP 缓冲区限制导致语音发送失败

**问题：** 语音 Base64 数据 (~68KB) 超过 STOMP 默认 64KB 缓冲区限制

**解决：**
```java
@Override
public void configureWebSocketTransport(WebSocketTransportRegistration registration) {
    registration.setMessageSizeLimit(1024 * 1024);   // 1MB
    registration.setSendBufferSizeLimit(1024 * 1024);
}
```

同时在 `application.yml` 中配置：
```yaml
spring:
  websocket:
    messaging:
      send-buffer-size-limit: 1048576
      message-size-limit: 1048576
  messaging:
    stomp:
      buffer-size-limit: 1048576
```

### 9.2 WebSocket 消息无法推送到接收方

**问题：** `convertAndSendToUser` 需要 Principal 来路由消息，但握手时未设置

**解决：** 在 `DefaultHandshakeHandler` 中设置 Principal
```java
.setHandshakeHandler(new DefaultHandshakeHandler() {
    @Override
    protected Principal determineUser(..., Map<String, Object> attributes) {
        Object user = attributes.get("currentUser");
        if (user instanceof User u) {
            final String userId = u.getId().toString();
            return () -> userId;
        }
        return null;
    }
})
```

### 9.3 快速切换视图导致内容混合

**问题：** 异步加载时，前一个请求未完成就发起新请求，导致内容混合

**解决：** Generation counter 模式
```javascript
let viewGeneration = 0;
function switchView(view) {
    const gen = ++viewGeneration;
    loadChatView(gen);
}
async function loadChatView(gen) {
    const isStale = () => gen !== viewGeneration;
    await fetch(...);
    if (isStale()) return;
}
```

### 9.4 N+1 查询性能问题

**问题：** 循环中逐个查询数据库

**解决：** 批量查询 + Map 缓存
```java
List<Long> ids = messages.stream().map(m -> m.getSenderId()).distinct().toList();
Map<Long, User> userMap = userRepository.findAllById(ids).stream()
    .collect(Collectors.toMap(User::getId, u -> u));
```

### 9.5 删除分组后好友 groupId 残留

**问题：** 删除分组后，好友记录中的 groupId 仍指向已删除的分组

**解决：** 删除前先将好友移至未分组
```java
@Transactional
public void deleteGroup(Long groupId) {
    List<Friend> friendsInGroup = friendRepository.findByGroupId(groupId);
    for (Friend friend : friendsInGroup) {
        friend.setGroupId(null);
        friendRepository.save(friend);
    }
    friendGroupRepository.deleteById(groupId);
}
```

### 9.6 文件名含空格导致 URL 访问失败

**问题：** 上传的文件名包含空格，浏览器无法正确访问

**解决：** 文件名 sanitization
```java
String safeFilename = originalFilename.replaceAll("[^a-zA-Z0-9_.\\-]", "_");
```

### 9.7 密码字段在 API 响应中泄露

**问题：** User 对象存储在 Session 中，API 返回时包含 password 字段

**解决：** 添加 `@JsonIgnore` 注解
```java
@JsonIgnore
@Column(nullable = false)
private String password;
```

---

## 十、总结与展望

### 10.1 项目成果

✅ **完成全部课程要求功能：**
- 用户注册、登录、登出、忘记密码、个人中心
- 好友搜索、申请、分组管理、移动、删除、备注
- 私聊（文本 + 语音）、消息状态、未读计数、搜索、导出
- 群聊创建、消息、成员管理、公告、邀请/入群申请、解散、导出
- 通知中心（好友申请 + 群聊邀请 + 入群申请统一管理）
- 后台管理系统（仪表盘、10张表CRUD、搜索、级联删除）
- 主题切换（亮色/暗色/跟随系统，全页面覆盖）

✅ **技术亮点：**
- WebSocket STOMP 实时通信（私聊点对点 + 群聊广播）
- MediaRecorder API 语音录制 + 文件存储优化
- BCrypt 密码加密 + Session 认证
- JPA 批量查询优化（避免 N+1）
- 异步竞态条件处理（Generation Counter）
- 统一响应格式封装（ApiResponse<T>）
- CSS 变量主题系统（三套主题全页面覆盖）
- 用户级联删除（9张表关联数据清理）

✅ **质量保证：**
- 56 个单元测试全部通过
- 测试覆盖率 ~78%
- 无明显安全漏洞
- 代码规范，注释清晰

### 10.2 工程认证指标点对应

| 毕业要求 | 指标点 | 本项目体现 |
|---------|--------|-----------|
| 毕业要求1 工程知识 | 1-4 软硬件系统研发与设计 | 完整的 Web 聊天系统架构设计与实现 |
| 毕业要求5 使用现代工具 | 5-1 资料查询与文献检索 | 参考微信/QQ 设计风格，查阅 Spring Boot/WebSocket 官方文档 |
| 毕业要求11 项目管理 | 11-2 工程管理原理与经济决策 | 团队分工协作，Git 版本控制，迭代开发 |

### 10.3 不足与改进方向

**当前不足：**
1. 缺少图片/文件消息支持（仅支持文本和语音）
2. 缺少消息撤回功能
3. 缺少在线状态实时更新
4. 缺少消息加密传输
5. 移动端适配不够完善

**未来改进方向：**
1. 增加图片/视频/文件消息类型
2. 实现消息撤回功能（2分钟内）
3. 使用 Redis 实现在线状态管理
4. 端到端消息加密
5. 响应式移动端适配
6. 增加表情包支持
7. 增加消息引用回复功能
8. 增加群组管理员权限分级

### 10.4 心得体会

通过本次大作业，我们深入学习了 Spring Boot Web 开发的全流程，包括：
- MVC 分层架构设计
- JPA/Hibernate ORM 映射
- WebSocket 实时通信技术
- Session 认证与安全
- 前端原生 JavaScript SPA 开发
- 音频处理与 Base64 编码
- 性能优化（N+1 查询、异步竞态）
- 单元测试与集成测试

项目开发过程中遇到的问题和解决方案，为我们积累了宝贵的工程实践经验。

---

## 附录

### A. 项目访问地址
- 线上地址：（待填写）
- 代码仓库：https://github.com/zhujiaozi/webtest-ppchat

### B. 团队成员分工

| 姓名 | 负责模块 | 主要工作 |
|------|---------|---------|
| 陈弦 | 模块1：账户系统 | 注册、登录、个人中心、密码管理 |
| 齐睿 | 模块2：好友管理 | 好友分组、好友申请、通知中心 |
| 沈越 | 模块3：群聊管理 | 群聊、群主、公告、邀请/入群申请 |
| 宋佳伟 | 模块4：消息系统 | 私聊/群聊消息收发、WebSocket |
| 郭峰 | 模块5：语音消息 | 语音录制、发送与储存、项目统筹 |
| 李子旸 | 模块6：后台管理 | Admin后台、前端UI、测试 |

### C. 参考文献
1. Spring Boot 官方文档：https://spring.io/projects/spring-boot
2. WebSocket STOMP 协议：https://stomp.github.io/
3. MDN MediaRecorder API：https://developer.mozilla.org/en-US/docs/Web/API/MediaRecorder
4. BCrypt 算法：https://en.wikipedia.org/wiki/Bcrypt
